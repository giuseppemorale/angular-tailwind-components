/**
 * How much room the card gives its content: `comfortable` (24px) for a page with a handful of
 * cards, `compact` (16px) for dashboards where the padding repeats across a dozen tiles.
 */
export type TailwindCardDensity = 'comfortable' | 'compact';
