import type { TailwindSize } from '../../../models';

/** Star glyph size per control size. */
export const STAR_PIXEL_SIZE: Record<TailwindSize, number> = {
  xs: 16,
  sm: 18,
  md: 22,
  lg: 26,
  xl: 32
};
