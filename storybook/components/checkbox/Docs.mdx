import { ArgTypes, Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as CheckboxStories from './checkbox.stories';

<Meta of={CheckboxStories} />

# Checkbox

`TailwindCheckbox` is a checkbox control wired into reactive forms through `ControlValueAccessor`.

---

## Import

```typescript
import { TailwindCheckbox } from 'angular-tailwind-components';
```

## Properties

<ArgTypes of={CheckboxStories} />

---

## Example

<Canvas of={CheckboxStories.Checkbox} />

```typescript
@Component({
  imports: [TailwindCheckbox],
  template: ` <tailwind-checkbox label="Accept terms" size="md" [checked]="false" />`
})
export class ExampleComponent {}
```
