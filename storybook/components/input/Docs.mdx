import { ArgTypes, Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as InputStories from './input.stories';

<Meta of={InputStories} />

# Input

`TailwindInput` is a text field with label, error states and `ControlValueAccessor` integration.

---

## Import

```typescript
import { TailwindInput } from 'angular-tailwind-components';
```

## Properties

<ArgTypes of={InputStories} />

---

## Example

<Canvas of={InputStories.Input} />

```typescript
@Component({
  imports: [TailwindInput],
  template: ` <tailwind-input label="Email" placeholder="Enter your email" type="email" size="md" [(value)]="email" />`
})
export class ExampleComponent {
  email = '';
}
```
