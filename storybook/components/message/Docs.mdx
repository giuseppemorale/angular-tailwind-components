import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as MessageStories from './message.stories';

<Meta of={MessageStories} />

# Message

`TailwindMessage` è un messaggio inline compatto con colore semantico e contenuto proiettato.

---

## Utilizzo

```typescript
@Component({
  imports: [TailwindMessage],
  template: `
    <tailwind-message color="info" icon="information-circle">
      This is an informational message.
    </tailwind-message>`
})
export class ExampleComponent {}
```

## Anteprima

<Canvas of={MessageStories.Message} />

---

## Properties

### `TailwindMessage (tailwind-message)`

Eredita `TailwindComponent` (`id`, `class`).

<table class="w-full">
  <thead>
    <tr>
      <th>Proprietà</th>
      <th>Tipo</th>
      <th>Default</th>
      <th>Descrizione</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>color</code></td>
      <td><code>TailwindColor</code></td>
      <td><code>'info'</code></td>
      <td>Colore semantico di sfondo e bordo.</td>
    </tr>
    <tr>
      <td><code>icon</code></td>
      <td><code>TailwindHeroicon</code></td>
      <td>—</td>
      <td>Nome icona Heroicons (outline); se omesso, nessuna icona a sinistra.</td>
    </tr>
    <tr>
      <td><code>iconSize</code></td>
      <td><code>number</code></td>
      <td><code>20</code></td>
      <td>Dimensione icona in px quando <code>icon</code> è impostato.</td>
    </tr>
  </tbody>
</table>

Il testo del messaggio si passa come contenuto proiettato. L’icona non è derivata da <code>color</code>: va scelta esplicitamente con <code>icon</code>.
