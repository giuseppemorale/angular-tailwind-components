import { ArgTypes, Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as SkeletonStories from './skeleton.stories';

<Meta of={SkeletonStories} />

# Skeleton

`TailwindSkeleton` renders animated placeholders shaped as text, circle, rectangle or rounded block.

---

## Import

```typescript
import { TailwindSkeleton } from 'angular-tailwind-components';
```

## Properties

<ArgTypes of={SkeletonStories} />

---

## Example

<Canvas of={SkeletonStories.Skeleton} />

```typescript
@Component({
  imports: [TailwindSkeleton],
  template: ` <tailwind-skeleton variant="text" width="100%" height="2rem" />`
})
export class ExampleComponent {}
```
