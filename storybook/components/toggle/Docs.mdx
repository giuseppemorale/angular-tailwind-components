import { ArgTypes, Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as ToggleStories from './toggle.stories';

<Meta of={ToggleStories} />

# Toggle

`TailwindToggle` is an on/off switch wired to `ControlValueAccessor`.

---

## Import

```typescript
import { TailwindToggle } from 'angular-tailwind-components';
```

## Properties

<ArgTypes of={ToggleStories} />

---

## Example

<Canvas of={ToggleStories.Toggle} />

```typescript
@Component({
  imports: [TailwindToggle],
  template: ` <tailwind-toggle
    label="Enable notifications"
    ariaLabel="Toggle notifications"
    size="md"
    [(checked)]="on" />`
})
export class ExampleComponent {
  on = false;
}
```
